services:
  - type: web
    name: tender-tool
    env: python
    buildCommand: apt-get update --fix-missing && apt-get install -y tesseract-ocr poppler-utils && pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
